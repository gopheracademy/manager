name: testing
on: [push]
jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-go@v1
      with:
        go-version: '1.15.5'
    - run: go run hello.go
